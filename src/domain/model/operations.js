export const sum = (a, b) => parseInt(a) + parseInt(b)
export const sub = (a, b) => parseInt(a) - parseInt(b)
export const multi = (a, b) => parseInt(a) * parseInt(b)
export const div = (a, b) => parseInt(a) / parseInt(b)
